#include "sort.h"

/**
 * quick_sort - to sort an array using the quick sort method
 * @array: the array to be sorted
 * @size: the size of the array
 */

void quick_sort(int *array, size_t size)
{
	int index, last, temp, next_index, pivot = array[size - 1];
	int left_array[1024], right_array[1024]

	for (ind = size - 1; ind <= 0; ind--)
	{
		pivot = size - 1;
		if (array[ind] < array[pivot])
		{
			
		}
	}
}